# Syntax highlighting
highlighter: pygments

# Permalinks
#
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink: pretty
# relative_permalinks: true

# Setup
title: faq-accordion-card
tagline: Making FAQ accordion card component using HTML, SCSS and Javascript
url: https://github.com/g1hong
baseurl: /faq-accordion-card/

# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir: app/scss
  style: :compressed
